export default class WeatherModel {
  constructor(props) {
    this.cityId = props.cityId;
    this.cityName = props.cityId;
    this.countryName = props.countryName;
    this.temprature = props.temprature;
    this.weatherTerm = props.weatherTerm;
  }
}
